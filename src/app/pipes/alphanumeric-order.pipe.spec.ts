import { AlphanumericOrderPipe } from './alphanumeric-order.pipe';

describe('AlphanumericOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphanumericOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
